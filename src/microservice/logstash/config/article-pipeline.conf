input {
  jdbc {
    jdbc_connection_string => "jdbc:mariadb://altair.latera.kr:3306/sibylla?useUnicode=true&characterEncoding=utf8"
    jdbc_user => "sibylla_admin"
    jdbc_password => "testament@@#"
    jdbc_driver_library => "/usr/share/logstash/config/libs/mariadb-java-client-2.3.0.jar"
    jdbc_driver_class => "org.mariadb.jdbc.Driver"
    statement => "SELECT * FROM article where reg_date > :sql_last_value"
    schedule => "* * * * *"
  }
}

output {
  elasticsearch {
    hosts => ["172.18.0.10:9200"]
    index => "article"
  }
  stdout {
    codec => rubydebug
  }
}
