input {
  jdbc {
    jdbc_connection_string => jdbc:mariadb://localhost:3306/sibylla?useUnicode=true&characterEncoding=utf8
    jdbc_user => "sibylla_admin"
    jdbc_password => testament@@#
    jdbc_driver_library => /usr/share/logstash/config/libs/mariadb-java-client-2.3.0.jar
    jdbc_driver_class => org.mariadb.jdbc.Driver
    statement => "SELECT * FROM article"
  }
}

output {
  stdout {
    codec => rubydebug
  }
}
